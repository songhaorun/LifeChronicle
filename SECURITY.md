# 安全政策

## 支持范围

LifeChronicle 当前处于 bootstrap 和非生产开发阶段，尚无受支持的正式发行版。

| 版本 | 安全支持 |
| --- | --- |
| `main` 最新提交 | 接受报告并尽力修复 |
| 未发布分支、个人 fork | 不承诺支持 |
| 正式发行版 | 尚不存在 |

仓库建立正式发布后，本表必须随发布策略更新。

## 私下报告漏洞

不要为漏洞创建公开 issue、PR、讨论或示例站点。

请发送邮件至 `1963535597@qq.com`，标题以
`[LifeChronicle Security]` 开头。初次报告不要附带真实个人数据、生产 Secret、
可直接滥用的凭据或不必要的大型日志。建议包含：

- 漏洞类型、影响和受影响组件；
- 复现所需的最小版本、commit、配置和步骤；
- 是否涉及身份、签名、ACK、位置、健康、删除、公开面或插件沙箱；
- 已采取的安全停止措施；
- 可公开的联系方式和期望披露方式。

如邮件渠道本身可能扩大风险，请先发送不含利用细节的联络请求，再协商加密传输方式。

## 响应目标

项目将尽力：

- 72 小时内确认收到报告；
- 7 个自然日内完成初步影响分级或请求必要信息；
- 在确认后给出临时缓解、修复与协调披露计划；
- 在修复发布后致谢报告者，除非报告者希望匿名或法律不允许。

这些是响应目标而非服务等级保证。bootstrap 阶段只有一名维护者时，复杂问题可能需要
外部独立专家评审。

## 优先报告的问题

- 设备身份、密钥、签名、Nonce 或 exact retry 绕过；
- 未满足持久确认条件却返回成功 ACK；
- OIDC、Scope、OPA、Share Token 或撤销失败；
- Public API、Card、Snapshot 或 CDN 能访问私有事实；
- 删除、暂停、保留和备份生命周期失效；
- 插件逃逸、越权 host call 或资源隔离失效；
- Secret、Token、精确位置、健康值或用户文本进入 Git、日志、trace、metric 或制品；
- 依赖投毒、构建来源伪造、不可追溯制品或许可证欺骗。

## 安全研究边界

欢迎善意、最小影响的安全研究。请：

- 只测试你拥有或明确获准的环境与数据；
- 优先使用本地、隔离、合成数据；
- 不进行拒绝服务、持久化、横向移动、社工或第三方账户访问；
- 一旦确认敏感数据可访问，停止读取并报告；
- 不公开利用细节，直到修复和协调披露时间达成一致；
- 遵守适用法律、上游许可证和平台条款。

本政策不授予访问第三方系统、绕过法律限制或获取数据的权限，也不构成法律豁免保证。

## 修复与披露

安全修复必须最小化变更，保留证据，补充回归测试，并记录受影响版本、缓解、升级和
回滚条件。涉及项目契约不可豁免不变量的问题不能通过临时例外永久放行。

披露内容不得包含 Secret、真实个人数据或能不必要扩大风险的细节。若上游依赖受影响，
维护者将按其安全政策协调报告。
