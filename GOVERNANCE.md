# LifeChronicle 治理

> - 生效日期：2026-07-27
> - 当前模式：bootstrap / 长期单维护者
> - 项目许可证：MIT

## 1. 目标与权威文件

本文件定义项目角色、所有权、决策、评审、发布和治理模式切换。工程不变量、阶段门、
例外和完成定义以
[项目契约](docs/contract/project-contract.md) 为准；架构与线上语义分别以总体架构、
事件规范和基础设施规范为准。治理规则不能被用来放宽这些权威契约。

发生冲突时：

1. 安全、隐私和数据保护采用更严格且失败关闭的规则；
2. 线上协议由对应版本化规范裁决；
3. 跨模块架构由已接受 ADR 裁决；
4. 其余治理问题由维护者记录决定和理由。

## 2. 当前角色登记

| 角色 | 当前负责人 | 权限与责任 |
| --- | --- | --- |
| Bootstrap maintainer | 宋昊润（GitHub `@songhaorun`；`1963535597@qq.com`） | 仓库治理、普通可逆变更自审、角色登记和模式切换 |
| Copyright holder | 宋昊润 | 选择项目自有内容的 MIT License |
| Release owner | 宋昊润 | 批准 tag、制品和发布记录；当前无正式发行版 |
| Security contact | 宋昊润（见 `SECURITY.md`） | 私下接收、分级和协调安全报告 |
| Contract/Data/Security/Platform owner | Bootstrap maintainer 临时兼任 | 仅限非生产 bootstrap；不得借兼任放宽不可豁免不变量 |

`CODEOWNERS` 使用已验证的 GitHub 账号 `@songhaorun`。增加维护者或切换托管平台时，
必须同步更新角色登记和所有权规则，不能保留托管平台无法识别的 owner。

## 3. Bootstrap 模式

项目采用 bootstrap/单维护者模式，并允许在没有第二名维护者的情况下长期运行；增加
第二名维护者不是阶段完成、日常开发或非生产发布的前置条件。保护分支仍强制通过 PR、
适用 CI、线性历史和对话解决，但不要求无法取得的非作者或 CODEOWNERS 批准。

Bootstrap maintainer 可以：

- 提交 MIT License 和可逆仓库脚手架；
- 对普通、可逆变更执行并记录自审；
- 建立 issue、PR、来源登记、ADR 和阶段门；
- 在当前实际存在的检查通过后自行合并普通、可逆的非生产变更。

Bootstrap maintainer 不得：

- 宣称不存在的 PR 批准、CI、分支保护、测试或阶段门已通过；
- 自行批准放宽身份、签名、持久 ACK、删除、Public 物理隔离、Secret 或失败关闭；
- 未经独立合格评审执行生产高风险或不可逆迁移；
- 未完成来源与许可证审查就引入第三方代码、制品或数据；
- 对外公开真实用户数据。

每次 bootstrap 自审至少记录：

```text
变更/工作包 ID：
维护者与日期：
范围与非目标：
实际运行的检查及结果：
尚未实现或无法运行的门禁：
隐私、安全、第三方和兼容评估：
回滚方式与剩余风险：
结论：
```

PR 模板作为该记录。CODEOWNERS 在单维护者模式下用于所有权登记和变更路由，不形成
作者无法满足的批准门槛。

## 4. 多人治理角色

进入多人治理后使用以下角色：

- **Maintainer**：管理仓库、版本、治理和最终合并；
- **Module owner**：维护指定模块的设计、质量和运行边界；
- **Contract owner**：负责 Proto、Registry、Topic、API、WIT 和兼容迁移；
- **Data owner**：负责事实源、迁移、保留、删除、备份和恢复；
- **Security/Privacy owner**：负责身份、密钥、授权、公开面、隐私和插件沙箱；
- **Platform owner**：负责基础设施、GitOps、可观测性、容量和生产恢复；
- **Release owner**：核对版本、制品、测试、迁移和回滚证据。

一人可以承担多个角色，但高风险变更所需的独立评审不能由同一人重复计数。Owner
变更必须同时更新本文件与 `CODEOWNERS`。

## 5. 决策与评审

### 5.1 普通变更

普通、可逆且不改变跨模块契约的变更由对应 owner 评审。多人模式下至少一名非作者
批准；单维护者模式由维护者通过 PR 记录自审，并以适用 CI 成功作为合并前置条件。

### 5.2 契约和架构

以下变化必须先有 ADR 或更新既有 ADR：

- 原始不可变、Event/Series、WAL/Outbox、事件时间或 Public 隔离；
- 核心数据库、消息总线、流处理、工作流、策略或插件运行时；
- 语言边界、顶层模块所有权、部署形态或跨服务依赖方向；
- Proto、Registry、Topic、API、WIT、DDL 和签名 framing 的破坏性变化。

多人模式下，契约变更还需受影响 consumer owner；安全、隐私、删除和公开变化还需
Security/Privacy owner。已接受 ADR 不改写历史，由新 ADR supersede。

### 5.3 高风险与例外

例外必须最小、限时、可撤销，有补偿控制、监控、owner、到期日和清除变更。项目契约
列出的不可豁免不变量不能批准例外。Bootstrap 模式不具备必要独立评审时，高风险动作
保持阻塞。

## 6. 分支、合并与所有权

- `main` 是目标集成分支；bootstrap 完成后禁止直接推送；
- 分支和提交格式遵循
  [提交规范](docs/governance/commit-convention.md)；
- PR 默认 squash merge，已发布 tag 不得移动；
- `CODEOWNERS` 覆盖默认规则、契约、安全、数据、基础设施及未来顶层模块；
- 作者不能批准自己的例外，机器人检查不能替代领域评审；
- 无关改动、用户本地配置和来源不明内容不得混入变更。

## 7. 第三方与研究治理

LifeChronicle 自有内容采用 MIT License，但第三方内容保留其原许可证。所有参考研究、
依赖、复制片段、资产、数据、协议材料和生成器都受
[第三方来源登记](THIRD_PARTY_SOURCES.md) 约束。

研究阶段允许在隔离临时环境运行官方项目以核实行为和许可证；这不授权复制或把其
依赖加入 LifeChronicle。产品纳入前必须有精确版本、逐文件来源、许可证兼容、通知、
专利/商标、分发/网络使用和退出方案审查。

## 8. 发布所有权

当前 release owner 是 bootstrap maintainer。尚无任何正式发行版或生产支持承诺。
正式发布必须：

- 使用不可移动的 SemVer tag；
- 记录唯一 commit、源码与制品 hash、依赖/SBOM 和构建环境；
- 记录实际测试、阶段门、迁移、Savepoint、策略和已知限制；
- 提供升级、回滚或前向修复条件；
- 核实 Secret 与敏感数据扫描；
- 核实第三方许可证、NOTICE/署名和来源登记；
- 由当前治理模式要求的 owner 批准。

文档或 tag 存在不等于发布门已通过。

## 9. 可选：转入多人治理

项目不主动承诺增加第二名维护者。只有未来确实有第二名维护者接受职责时，才由版本化
治理变更宣布切换；切换前继续使用第 3 节的长期单维护者规则。切换必须满足：

1. 至少两名已登记维护者接受职责；
2. 远程仓库存在并以 `main` 为默认分支；
3. `main` 分支保护和必需检查生效；
4. 托管平台已识别 `CODEOWNERS`；
5. CI 能运行当前阶段适用检查；
6. 安全、发布、Contract、Data 和 Platform owner 已登记；
7. 未决 bootstrap 自审、风险和缺失门禁已盘点。

切换后，项目契约中的非作者和多人批准要求立即完整生效，不得继续使用 bootstrap
条款绕过评审。

## 10. 治理变更

治理变更必须列出受影响角色、权限、阶段门、迁移和生效日期。多人模式下至少两名
维护者批准；涉及安全、隐私或生产发布时还需对应 owner。紧急止血只能做最小动作，
并在 24 小时内回写 Git 或撤销。
